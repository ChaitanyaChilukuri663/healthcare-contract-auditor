# Runtime dependencies. Install with:  pip install -r requirements.txt
# (Versions pinned to a known-good set.)

fastapi==0.136.3
uvicorn[standard]==0.49.0
streamlit==1.58.0
pydantic==2.13.4
pydantic-settings==2.14.1

# LLM provider abstraction (OpenAI-compatible client for github/azure/groq)
openai==2.41.0
langchain==1.3.4
langchain-openai==1.2.2

python-dotenv==1.2.2
httpx==0.28.1

# Document extraction
pypdf==6.13.2
tiktoken==0.13.0

# Azure services (full pipeline)
azure-search-documents==12.0.0
azure-storage-blob==12.30.0
azure-identity==1.25.3

# Azure SQL (async)
aioodbc==0.5.0
pyodbc==5.3.0
aiohttp==3.14.1

# Data processing / fuzzy matching
pandas==3.0.3
numpy==2.4.6
rapidfuzz==3.14.5
