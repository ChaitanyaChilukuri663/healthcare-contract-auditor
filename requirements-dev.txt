# Dev/test tools. Install with:  pip install -r requirements-dev.txt
# (Includes the runtime deps via the -r line below.)

-r requirements.txt

pytest==9.0.3
pytest-asyncio==1.4.0
pytest-mock==3.15.1
ruff==0.15.16
# The [nodejs] extra ships Node.js as a PyPI wheel so pyright doesn't try to
# download Node from nodejs.org (blocked by the corporate proxy here).
pyright[nodejs]==1.1.410
# Used by data/generate_synthetic_contracts.py to build the sample PDFs.
reportlab==4.5.1
