# docs/

Put a screenshot or GIF of the running Streamlit app here as **`demo.png`** (or `demo.gif`).

It's referenced from the top-level `README.md` under **Demo** — uncomment the image line there
once the file exists. A quick capture of the UI showing **Provider A → PASS** and
**Provider B → FAIL** makes a great portfolio/LinkedIn visual.
