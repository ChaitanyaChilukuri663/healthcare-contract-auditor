# docs/

Put screenshots/GIFs of the running Streamlit app here:
- **`demo.png`** — the single-contract audit (e.g. Provider A → PASS, Provider B → FAIL).
- **`dashboard.png`** — the portfolio dashboard (compliance rate, violations, $ exposure).

Both are referenced from the top-level `README.md` under **Demo** — uncomment the image
lines there once the files exist. Great portfolio/LinkedIn visuals.
